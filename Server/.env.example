# NexLink Relay Server — Environment Variables
# Copy this to .env and fill in your values

# Firebase Service Account JSON (paste the whole JSON as a single line)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"nexlink-62e41",...}

# Port (Render.com sets this automatically)
PORT=3000
